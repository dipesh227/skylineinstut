export interface Student {  id: string;  roll_number: string;  name: string;  email: string;  phone: string;  course_id: string;  course_name: string;  photo_base64?: string;  fee_amount: number;  fee_paid: number;  fee_balance: number;  reg_date: string;  valid_till: string;  created_at?: string;attendance_records?: StudentAttendance[];  results_records?: StudentResult[];  fee_ledgers?: FeeLedgerEntry[];}